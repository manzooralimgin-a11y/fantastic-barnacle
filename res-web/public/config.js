// Runtime configuration for res-web.
// This file is loaded before the app bundle and can be overwritten at deploy time
// to inject environment-specific values without rebuilding.
//
// In development, set VITE_PUBLIC_API_BASE_URL and VITE_RESTAURANT_ID in .env instead.
// In production, replace the values below (or inject via server-side templating).
window.RES_WEB_CONFIG = window.RES_WEB_CONFIG || {};
// window.RES_WEB_CONFIG.apiBaseUrl = 'https://your-backend.example.com/api';
// window.RES_WEB_CONFIG.restaurantId = 1;
